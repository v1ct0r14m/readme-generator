{
  "name": "static-site",
  "version": "1.0.0",
  "description": "command-line application that takes input from user to generate README",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "victoria mota",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.4"
  }
}
